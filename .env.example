# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.

APP_NAME=LeadSpawn
ENVIRONMENT=development
DEBUG=true

# Google Places API (New) key — see README for setup instructions
GOOGLE_PLACES_API_KEY=
